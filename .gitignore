node_modules/

# Credentials — never commit any of these.
.env
.env.*
!.env.example
*.key
*.pem

# Usage log: contains visitor metadata (region, timestamps).
logs/

# Session store: holds teens' actual conversations. Never commit it.
data/
*.db
*.db-wal
*.db-shm
